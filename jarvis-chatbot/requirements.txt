# Core
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
loguru>=0.7.2

# LLM (LM Studio OpenAI-compatible)
openai>=1.50.0
httpx>=0.27.0

# SQLite (long-term memory)
aiosqlite>=0.20.0

# Agent framework
langgraph>=0.2.50
langchain-core>=0.3.0

# RAG
chromadb>=0.5.15
sentence-transformers>=3.0.0
# transformers 5.x는 PyInstaller --onefile 시 _grouped_mm_can_dispatch에서
# inspect.getsource 호출로 깨진다. 4.x로 고정.
transformers>=4.41,<5.0
langchain-text-splitters>=0.3.0

# 문서 추출 (PDF/docx)
pypdf>=4.0.0
python-docx>=1.1.0

# PDF → PNG 렌더 (텔레그램 미리보기용)
pypdfium2>=4.30.0
Pillow>=10.0.0

# Telegram
python-telegram-bot>=21.6

# Dashboard
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0
jinja2>=3.1.0

# Tests
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Packaging (Phase 5)
# pyinstaller>=6.10.0
